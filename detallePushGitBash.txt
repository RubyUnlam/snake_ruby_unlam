git checkout develop <-- me paro en develop
git pull origin develop <-- traigo el remoto a mi pc (develop en este caso)
git checkout mi_feature
git merge develop (no necesario). Si lo hago, lo hago en mi branch. Dsp debería ir a develop y hacer git merge my_feature

git commit origin my_feature

1) pararme en mi branch
2) add al stage
3) git commit -m "mi mensajito"
4) git push origin (lo mando al remoto) my_branch
5) ir a github y pedir el pr
6) si lo aceptan, checkout develop y hacer el pull.


si hago un commit, ese pertenece al branch donde estoy parado.
Si tengo cosas stageadas y quiero hacer un checkout, o hago un stash (back up) (o los comiteo) (mandarlas al stage es al pedo xq igual me siguen al hacer el checkout (y se salen)).

para hacer un pull, me paro lo q quiero pullear, y mando git pull branch_al_que_quiero_hacerlo.

para crear un branch

git checkout -b nombre_del_branch